{:paths ["src" "test"]
 :tasks {test {:doc "Run the cljc contract + behavioral test under babashka"
               :extra-paths ["test"]
               :requires ([shopee.main-test])
               :task (let [{:keys [fail error]} (clojure.test/run-tests 'shopee.main-test)]
                       (System/exit (if (pos? (+ fail error)) 1 0)))}}}
